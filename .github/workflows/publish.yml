name: Publish

on:
  push:
    tags:
      - "v2.*"

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false

      - run: bun install --frozen-lockfile

      - name: Validate release tag and package graph
        run: |
          VERSION=$(node -p "require('./packages/shared/package.json').version")
          if [ "${GITHUB_REF_NAME}" != "v${VERSION}" ]; then
            echo "Tag ${GITHUB_REF_NAME} does not match package version ${VERSION}."
            exit 1
          fi

          node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const packagesDir = "packages";
          const packages = fs.readdirSync(packagesDir)
            .map((name) => path.join(packagesDir, name, "package.json"))
            .filter((file) => fs.existsSync(file))
            .map((file) => ({ file, manifest: JSON.parse(fs.readFileSync(file, "utf8")) }));

          const version = packages.find((pkg) => pkg.manifest.name === "@kilnai/integration-shared").manifest.version;
          const failures = [];

          for (const { file, manifest } of packages) {
            if (manifest.version !== version) {
              failures.push(`${file}: expected version ${version}, found ${manifest.version}`);
            }

            if (manifest.peerDependencies?.["@kilnai/core"] !== "^2.1.0") {
              failures.push(`${file}: peerDependencies.@kilnai/core must be ^2.1.0`);
            }

            for (const section of ["dependencies", "peerDependencies", "optionalDependencies"]) {
              for (const [name, range] of Object.entries(manifest[section] ?? {})) {
                if (name.startsWith("@kilnai/integration-") && range !== `^${version}` && range !== version && range !== "workspace:*") {
                  failures.push(`${file}: ${section}.${name} must target ${version}, found ${range}`);
                }
              }
            }
          }

          if (failures.length > 0) {
            console.error(failures.join("\n"));
            process.exit(1);
          }
          NODE

      - name: Typecheck
        run: bun run typecheck

      - name: Test
        run: bun run test

      - name: Build
        run: bun run build

      - name: Resolve workspace versions for publish
        run: |
          node <<'NODE'
          const fs = require("fs");
          const path = require("path");

          const packagesDir = "packages";
          const version = JSON.parse(fs.readFileSync("packages/shared/package.json", "utf8")).version;

          for (const packageName of fs.readdirSync(packagesDir)) {
            const manifestPath = path.join(packagesDir, packageName, "package.json");
            if (!fs.existsSync(manifestPath)) {
              continue;
            }

            const manifest = JSON.parse(fs.readFileSync(manifestPath, "utf8"));
            for (const section of ["dependencies", "peerDependencies", "optionalDependencies", "devDependencies"]) {
              const deps = manifest[section];
              if (!deps) {
                continue;
              }

              for (const name of Object.keys(deps)) {
                if (name.startsWith("@kilnai/integration-") && deps[name] === "workspace:*") {
                  deps[name] = `^${version}`;
                }
              }
            }

            fs.writeFileSync(manifestPath, `${JSON.stringify(manifest, null, 2)}\n`);
          }
          NODE

      - name: Publish @kilnai/integration-shared
        working-directory: packages/shared
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @kilnai/integration-google-calendar
        working-directory: packages/google-calendar
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @kilnai/integration-stripe
        working-directory: packages/stripe
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @kilnai/integration-google-sheets
        working-directory: packages/google-sheets
        run: npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
